import { CheckNameDirective } from './checkname.directive';

describe('CheckNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckNameDirective();
    expect(directive).toBeTruthy();
  });
});
