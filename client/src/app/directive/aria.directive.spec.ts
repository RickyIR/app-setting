import { AriaDirective } from './aria.directive';

describe('AriaDirective', () => {
  it('should create an instance', () => {
    const directive = new AriaDirective();
    expect(directive).toBeTruthy();
  });
});
